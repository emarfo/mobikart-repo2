version: 0.2

phases: 
    build:
        commands:
            - echo "Build block"
            - ls
       post_build:
        commands:
            - echo "Post build phase"
artifacts:
    files:
        - scripts/start_server
        - scripts/stop_server
        - dist/*
        - appspec.yml
    discard-paths: yes
